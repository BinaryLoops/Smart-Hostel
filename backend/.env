PORT="4000"

# Set to your frontend origin in production (Netlify URL), e.g. https://your-site.netlify.app
CORS_ORIGIN=" "

# Firebase Admin (required for Firestore persistence + verified auth)
FIREBASE_PROJECT_ID=" "

# Paste the Firebase service account JSON here as a SINGLE LINE string
FIREBASE_SERVICE_ACCOUNT_JSON=" "

